<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyNotes.NotePage"
             xmlns:mynotes="clr-namespace:MyNotes">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Editor x:Name="editor" Text="{Binding Text}"  VerticalOptions="FillAndExpand" 
                    TextColor="{Binding TextColor}" FontSize="{Binding FontSize}"/>
            <Button Text="Save" TextColor="White" FontAttributes="Bold" Clicked="Save_Clicked" 
                    BackgroundColor="#291A4C" BorderColor="#2A1E48" BorderWidth="2" Margin="2"
                    VerticalOptions="End" HorizontalOptions="Fill"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>